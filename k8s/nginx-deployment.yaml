apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: mkjs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: ghcr.io/matheusperillo03/mkjs-nginx:latest
          ports:
            - containerPort: 80
